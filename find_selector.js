const fs = require('fs');
const React = require('react');
// Actually, rendering the React app to string and using a DOM parser is best.
// But we don't have all the setup for that easily without transpiling.
